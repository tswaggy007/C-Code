#include <stdio.h>
#include <float.h>
/*Program for Temperature Conversion*/
int main(void)
{
  printf("C:");

  float celsius = float();
  float fahrenheit = ((celsius * 9) / 5) + 32;

  printf("F: .01%f", fahrenheit);

  return 0;
}
